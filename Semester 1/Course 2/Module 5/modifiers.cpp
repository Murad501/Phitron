#include <bits/stdc++.h>
using namespace std;

int main()
{
    string a = "Hello";
    string b = "World";

    // a+=b;
    // a.append(b);

    // a.push_back('A');
    // a.pop_back();
    // a.pop_back();

    // a.erase(1, 2);
    // a.replace(1, 2, "ii");
    a.insert(4, "Murada");
    cout<<a<<endl;


    return 0;
}